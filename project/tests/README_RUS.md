# Тесты проекта

Для **тестирования** мы используем `pytest`.

Чтобы провести тестирование, выполните
```bash
cd project
pytest
```

Ниже представлен список тестов в этой директории и их описания:
  - `test_api_service.py`: тестирование эндпоинтов `"/"`, `"/health"` и `"/upload"`.